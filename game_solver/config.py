from __future__ import annotations

# 固定版式参数
REF_W = 940.0
REF_H = 2048.0
GRID_ROWS = 52
GRID_COLS = 38

X_EDGE0_REF = 67.84210526
X_STEP_REF = 21.19203414
Y_EDGE0_REF = 89.26197388
Y_STEP_REF = 18.34568428

FRONT_X_N = [0.2553, 0.4181, 0.5819, 0.7447]
FRONT_Y_N = 0.7969
NEXT_Y_N = 0.8960

# 第一排队列列数/横坐标动态检测。
# 只扫描第一排车顶数字所在窄带，因此 4列、5列以及后续其它列数都不再写死。
FRONT_DIGIT_SCAN_X1_N = 0.04
FRONT_DIGIT_SCAN_X2_N = 0.90
FRONT_DIGIT_SCAN_Y1_N = 0.75
FRONT_DIGIT_SCAN_Y2_N = 0.80

FRONT_DIGIT_SAT_MAX = 105
FRONT_DIGIT_VAL_MIN = 150
FRONT_DIGIT_COMPONENT_MIN_AREA = 80.0
FRONT_DIGIT_COMPONENT_MAX_AREA = 1000.0
FRONT_DIGIT_COMPONENT_MIN_H = 22.0
FRONT_DIGIT_COMPONENT_MAX_H = 50.0
FRONT_DIGIT_COMPONENT_MIN_W = 4.0
FRONT_DIGIT_COMPONENT_MAX_W = 40.0
FRONT_DIGIT_MAX_ASPECT = 1.20

FRONT_DIGIT_PAIR_MIN_HEIGHT_SIM = 0.72
FRONT_DIGIT_PAIR_MAX_Y_OFFSET_RATIO = 0.28
FRONT_DIGIT_PAIR_MAX_GAP = 22.0
FRONT_DIGIT_PAIR_MAX_SPAN = 78.0

PARK_Y1_N = 0.55
PARK_Y2_N = 0.68
PARK_X1_N = 0.03
PARK_X2_N = 0.83

DEFAULT_SLOTS = 6

PALETTE_DBSCAN_EPS = 30.0
PALETTE_DBSCAN_MIN_SAMPLES = 4
PALETTE_MIN_CLUSTER_SIZE = 4
KEEP_COLOR_DIST = 60.0
INITIAL_COLOR_DIST = 55.0
CAR_COLOR_DIST = 70.0
NEW_COLOR_APPEND_DIST = 85.0

# 完整分析阶段：停车数字锚点
PARK_DIGIT_MONITOR_X1_N = 0.035
PARK_DIGIT_MONITOR_X2_N = 0.92
PARK_DIGIT_MONITOR_Y1_N = 0.485
PARK_DIGIT_MONITOR_Y2_N = 0.625
PARK_DIGIT_MONITOR_SAT_MAX = 100
PARK_DIGIT_MONITOR_VAL_MIN = 155

PARK_DIGIT_COMPONENT_MIN_AREA = 85.0
PARK_DIGIT_COMPONENT_MAX_AREA = 1000.0
PARK_DIGIT_COMPONENT_MIN_H = 22.0
PARK_DIGIT_COMPONENT_MAX_H = 60.0
PARK_DIGIT_COMPONENT_MIN_W = 4.0
PARK_DIGIT_COMPONENT_MAX_W = 42.0

PARK_DIGIT_PAIR_MIN_HEIGHT_SIM = 0.68
PARK_DIGIT_PAIR_MAX_Y_OFFSET_RATIO = 0.32
PARK_DIGIT_PAIR_MAX_GAP = 24.0
PARK_DIGIT_PAIR_MAX_SPAN = 72.0
PARK_DIGIT_SHAPE_MIN_SCORE = 0.70

# 分流监控阶段：真正“纯数字像素”，不调用任何 OCR / 模板库。
# 这个窄带覆盖停车车顶数字，包含最右第6车位；
# 通过 Y 窄带 + 字符几何过滤避开速度按钮/解锁UI，而不是裁掉右侧车位。
RAW_MONITOR_X1_N = 0.06
RAW_MONITOR_X2_N = 0.92
RAW_MONITOR_Y1_N = 0.556
RAW_MONITOR_Y2_N = 0.606

RAW_MONITOR_SAT_MAX = 80
RAW_MONITOR_VAL_MIN = 220

RAW_MONITOR_COMPONENT_MIN_AREA = 70.0
RAW_MONITOR_COMPONENT_MAX_AREA = 1300.0
RAW_MONITOR_COMPONENT_MIN_H = 18.0
RAW_MONITOR_COMPONENT_MAX_H = 68.0
RAW_MONITOR_COMPONENT_MIN_W = 3.0
RAW_MONITOR_COMPONENT_MAX_W = 46.0
RAW_MONITOR_MAX_ASPECT = 1.30

RAW_MONITOR_MASK_CHANGE_RATIO = 0.08
RAW_MONITOR_EMPTY_CONFIRM_EXTRA_FRAMES = 2

# 快速 OCR
FAST_OCR_MIN_SCORE = 0.76
FAST_OCR_MIN_MARGIN = 0.035
FAST_OCR_STRONG_SCORE = 0.84
FAST_OCR_STRONG_MARGIN = 0.018
FRONT_FINGERPRINT_CHANGE_RATIO = 0.045

# 状态格式与 v4 兼容，允许同一局继续。
STATE_VERSION = 3

UNKNOWN = -1
EMPTY = 0


# ------------------ 每局开始自动解锁第6停车位 ------------------
# 940x2048 实机截图中“解锁”按钮中心约 (728, 1115)。
SIXTH_SLOT_UNLOCK_X_N = 0.875
SIXTH_SLOT_UNLOCK_Y_N = 0.617

# 用户提供的广告结束截图中，右上角关闭叉中心约 (762, 111)。
AD_CLOSE_X_N = 0.811
AD_CLOSE_Y_N = 0.0545

# 点击“解锁”后，广告至少播放约15秒才允许关闭。
UNLOCK_AD_MIN_WATCH_SECONDS = 15.5

# 点击解锁后先等广告页面切入；若此时仍能检测到游戏第一排，
# 说明广告没有打开（通常表示第6位已经解锁），不会盲等15秒。
UNLOCK_AD_OPEN_CHECK_DELAY = 1.2

# 关闭广告后等待游戏界面恢复。
UNLOCK_AD_RETURN_DELAY = 1.0

# 若15.5秒后关闭按钮尚未真正生效，每隔1秒重试一次，最多8次。
UNLOCK_AD_CLOSE_RETRY_INTERVAL = 1.0
UNLOCK_AD_CLOSE_RETRY_COUNT = 8


# 广告/游戏界面区分只用于“新局广告解锁”流程：
# 新局第一排至少应有3列，且横向覆盖一定宽度。
UNLOCK_GAME_FRONT_MIN_COLUMNS = 3
UNLOCK_GAME_FRONT_MIN_SPAN_N = 0.35

# v5.19: 3列关卡的第一排横向跨度天然小于4/5列。
# 关卡16实测三列中心约 x=316,471,624，span_n≈0.328，
# 原统一 0.35 阈值会把正常游戏界面误判为广告/其它界面。
# 三列时单独放宽跨度，同时要求相邻列间距近似规则。
UNLOCK_GAME_THREE_COL_MIN_SPAN_N = 0.30
UNLOCK_GAME_THREE_COL_MAX_GAP_RATIO = 1.35


# “解锁”按钮动态搜索区域。
# 实机按钮暗色主体约 115x155（940x2048 基准），固定出现在停车区右侧。
UNLOCK_BUTTON_SEARCH_X1_N = 0.55
UNLOCK_BUTTON_SEARCH_X2_N = 0.98
UNLOCK_BUTTON_SEARCH_Y1_N = 0.54
UNLOCK_BUTTON_SEARCH_Y2_N = 0.69

UNLOCK_BUTTON_DARK_GRAY_MAX = 125
UNLOCK_BUTTON_MIN_W_REF = 90
UNLOCK_BUTTON_MAX_W_REF = 150
UNLOCK_BUTTON_MIN_H_REF = 125
UNLOCK_BUTTON_MAX_H_REF = 185
UNLOCK_BUTTON_MIN_AREA_REF = 8500
UNLOCK_BUTTON_MAX_AREA_REF = 18000
UNLOCK_BUTTON_MIN_ASPECT = 0.55
UNLOCK_BUTTON_MAX_ASPECT = 0.90